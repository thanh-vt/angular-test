import { QuestionIdPipe } from './question-id.pipe';

describe('QuestionIdPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionIdPipe();
    expect(pipe).toBeTruthy();
  });
});
